// Salary parsing and statistics (all values EUR)
// Implemented in WI-010 (Phase 2, Track B — Bolt)

export function parseSalary(_salaryString) {
  // Placeholder — returns null
  // Bolt will implement: ranges, single values, currency symbols, "k" suffix
  return null
}

export function analyzeSalaries(_jobs) {
  // Placeholder — returns empty stats
  // Bolt will implement: min, max, average, median across parseable listings
  return { min: null, max: null, average: null, median: null, data: [] }
}
