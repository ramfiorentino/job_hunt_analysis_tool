// Keyword extraction and frequency analysis
// Implemented in WI-006 (Phase 2, Track A — Atlas)

export function normalizeKeyword(keyword) {
  return keyword.trim().toLowerCase()
}

export function getKeywordFrequencies(_jobs) {
  // Placeholder — returns empty array
  // Atlas will implement: split on commas, normalize, count, sort descending
  return []
}
