// Location and remote status analysis
// Implemented in WI-012 (Phase 2, Track B — Bolt)

export function analyzeLocations(_jobs) {
  // Placeholder — returns empty breakdown
  // Bolt will implement: Remote/Hybrid/On-site categorization from Remote Status column,
  // city grouping and normalization from Location column
  return { remoteBreakdown: [], cityBreakdown: [] }
}
